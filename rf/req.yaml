name: rf_demo
channels:
  - conda-forge
dependencies:
  # core runtime
  - python=3.10
  - pip            # lets conda track pip‑installed wheels
  # data handling
  - pandas
  - numpy
  # web map + UI
  - folium
  - pip:
      - gradio==4.*     # pulls the latest v4 series from PyPI